---
title: "Stores — named mounts, partitions, files"
description: "StoresResource: create, list, get, update, delete, listFiles, writeFile, getFile, deleteFile"
sdk: "sdk"
version: "0.7.5"
prev: "/packages/sdk/v0.7.5/resources/memory"
next: "/packages/sdk/v0.7.5/resources/files"
related: ["/packages/sdk/v0.7.5/resources/memory", "/packages/sdk/v0.7.5/resources/agents"]
---

# Stores

`client.stores` → `/api/v1/developer/stores` — named mounts generalization of memory (`/stores/<name>/`).

```ts
const store = await persona.stores.create{});
await persona.stores.writeFile(store._id,{});
const files: StoreFile[] = await persona.stores.listFiles(store._id);
```

Related: [Memory scopes](/packages/sdk/v0.7.5/resources/memory)
