# Persona Developer Platform Project credentials (Project settings → API
# credentials). Server-side only — never exposed to the browser.
PERSONA_BASE_URL=https://api.persona.hasanraiyan.me
PERSONA_CREDENTIAL=your-key-id.your-secret

# Which Agent this demo chats with.
NEXT_PUBLIC_PERSONA_AGENT_ID=your-agent-id
