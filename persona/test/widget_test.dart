import 'package:flutter_test/flutter_test.dart';

// Smoke test placeholder — replace with real widget tests once auth screens
// are covered by integration tests.
void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
