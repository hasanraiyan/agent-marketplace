---
title: "useVoice"
description: "Real-time voice — Gemini Live"
---

# useVoice

See [Guide: Voice](/docs/react/v0.10.0/guides/voice). Full API: `state`, `transcript`, `partial`, `toolCalls`, `endReason`, `error`, `start`, `stop`, `mute`, `sendText`, `updateContext`.

`transcript` is live-only (no history replay). See `sdk/react/src/types.ts:407` `PersonaVoiceState` etc.
