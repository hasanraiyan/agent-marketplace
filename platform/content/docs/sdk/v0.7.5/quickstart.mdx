---
title: "Quickstart — @personaai/sdk"
description: "Install, auth, create agent, chat per external user"
---

# Quickstart

```ts
import { PersonaClient } from '@personaai/sdk';
const persona = new PersonaClient({
  baseUrl: 'https://api.persona.hasanraiyan.me',
  credential: process.env.PERSONA_CREDENTIAL!,
});
const who = await persona.whoami();
const agent = await persona.agents.create({ name: 'Demo', systemPrompt: 'You are helpful', providerId: '...' });
const userClient = new PersonaClient({
  baseUrl: 'https://api.persona.hasanraiyan.me',
  credential: process.env.PERSONA_CREDENTIAL!,
  externalUserId: 'user_123',
});
const thread = await userClient.threads.create({ agentId: agent._id });
const result = await userClient.chat.sendMessage(agent._id, { messages: [{ role:'user', content:'Hello' }] });
console.log(result.text);
```

Interlinked: [Agents](/docs/sdk/v0.7.5/guides/agents) • [Chat stream](/docs/sdk/v0.7.5/guides/chat) • [Adapters](/docs/adapters/v0.1.0)
