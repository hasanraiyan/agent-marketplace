# @personaai/react — docs source

Source MDX for `@personaai/react` lives here and is published into `platform/content/docs-v2/react/v{version}/` via `platform/scripts/snapshot-docs.mjs`.

- `hooks/*.mdx` — per-hook reference (hand-written, doplněno generated Types).
- `guides/*.mdx` — tasks (ephemeral chat, voice, streaming).

See `plan.md` §6 and §9.
